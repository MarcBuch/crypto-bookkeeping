{
  "name": "@lp-tracker/api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "check": "bun run tsc --noEmit",
    "test": "bun test --isolate src/test"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@lp-tracker/core": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
